import { createStore } from "vuex";
import reserve from './modules/reserve';

export default createStore({
    state:{
    },
    mutations: {
    },
    getters: {
    },
    modules: {
        reserve,
    },
});
